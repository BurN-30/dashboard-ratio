# ⚠️ IMPORTANT: NE PAS COMMITTER CE FICHIER S'IL CONTIENT DES VRAIS IDENTIFIANTS!
# Ce fichier est un EXEMPLE - À DUPLIQUER en `.env.local` et À REMPLIR

# ============================================================
# Frontend Configuration (Next.js)
# ============================================================

# Password pour accéder à la page /login
ADMIN_PASSWORD=your_admin_password_here

# URL de base où sont hébergés les fichiers JSON (stats.json, history.json)
# Ex: https://votresite.com/dash (sans slash à la fin)
JSON_BASE_URL=https://votresite.com/dash

# ============================================================
# Configuration Ngrok (Hardware Monitoring)
# ============================================================

# URL publique de votre tunnel ngrok (pour accès client-side)
# Cette variable est exposée au navigateur (NEXT_PUBLIC_)
NEXT_PUBLIC_NGROK_URL=https://votre-subdomain.ngrok-free.app

# URL publique de votre tunnel ngrok (pour accès serveur-side API routes)
NGROK_URL=https://votre-subdomain.ngrok-free.app

# ============================================================
# Optionnel : Protection FTP avec Basic Auth
# ============================================================

# Si votre dossier FTP /dash est protégé par .htaccess avec Basic Auth
# Décommentez et remplissez ces variables :

# Origin header pour les requêtes CORS
# JSON_ORIGIN=https://dash.votresite.com

# Identifiants Basic Auth (format: username:password)
# Seront automatiquement encodés en Base64 par l'API
# JSON_AUTH_BASIC=username:password

# ============================================================
# Optionnel : Token Hardware Monitor
# ============================================================

# Si vous avez activé l'authentification par token pour l'API hardware
# Décommentez et utilisez le même token que dans le .env du backend
# NEXT_PUBLIC_HWMONITOR_TOKEN=votre_token_ici

# ============================================================
# Notes:
# ============================================================
# 1. Dupliquer ce fichier en `.env.local` dans le même dossier
# 2. Remplir avec vos vraies valeurs
# 3. NE JAMAIS committer le `.env.local` dans Git
# 4. Le fichier `.env.local` est déjà dans .gitignore (vérifié)
# 5. Pour générer un mot de passe admin sécurisé : openssl rand -base64 32
#
# Variables utilisées :
# - ADMIN_PASSWORD : Mot de passe pour accéder au dashboard (page /login)
# - JSON_BASE_URL : URL publique où le scraper upload les JSON via FTP
# - NEXT_PUBLIC_NGROK_URL : Tunnel ngrok pour le monitoring hardware (client)
# - NGROK_URL : Même tunnel, mais pour les routes API Next.js (serveur)
